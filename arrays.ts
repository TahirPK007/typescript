const superhero:string[]=[]
const x:number[]=[]
superhero.push("spider")
x.push(2)

//another way of defining
const xx:Array<number>=[]

//
type userr={
    name:string
    isactive:boolean
}

const allusers:userr[]=[]
allusers.push({
    name:"ali",
    isactive:true
})

